[gcode_macro POWER_OFF_PRINTER]
description: Turn off switch for printer
gcode:
  {% set vars = printer["gcode_macro _CONFIGS"].shutdown.method %}
  {action_call_remote_method(vars.name,
    device=vars.device,
    state=vars.state)
  }
