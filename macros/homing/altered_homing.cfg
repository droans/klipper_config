[gcode_macro _OVERRIDE_HOME]
gcode:
    {% set axis = params.AXIS | lower %}

    {% set data = printer["gcode_macro _CONFIGS"].homing.axes[axis].override %}

    {% for gcode in data %}
        {gcode}
    {% endfor %}
    

    