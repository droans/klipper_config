[gcode_macro M604]
description: Complete Filament Change
gcode:
    {% set restore_state = printer["gcode_macro _CONFIGS"].fil_change_gcode_state_name %}

    RESTORE_GCODE_STATE NAME={restore_state} MOVE=1

    RESUME